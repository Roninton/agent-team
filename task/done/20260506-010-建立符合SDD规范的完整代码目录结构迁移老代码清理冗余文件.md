# 建立符合SDD规范的完整代码目录结构，迁移老代码，清理冗余文件

## 核心上下文摘要
### 参考规范
根据`docs/specs/architecture/tech-stack-and-architecture.md`与`backend-architecture.md`中的分层架构规范，代码必须按照分层模块化设计，目录结构清晰，职责单一。

### 现有基础
当前已有初始代码文件：
- `src/extension.ts`（VS Code扩展入口）
- `src/config/`（配置相关）
- `src/core/`（核心模块）
- `src/handlers/`（处理层）
- `src/ui/`（UI相关）
- `src/utils/`（工具类）
- `test/`（测试目录）

### 任务目标
1. 建立符合SDD分层架构的完整目录结构
2. 迁移现有有效代码到对应的正确位置
3. 删除无用的冗余文件、注释、调试信息
4. 补充必要的配置文件、脚手架代码、类型定义
5. 确保目录结构完全对齐SDD规范中的模块划分

---

## 验收标准
1. [x] 目录结构严格对齐SDD中的分层模块化设计，层级清晰，职责明确
2. [x] 现有有效代码已全部迁移到对应目录，功能不受影响
3. [x] 所有冗余文件、无用代码、调试信息已清理完毕
4. [x] 各模块目录下已补充必要的`README.md`说明文件，描述模块职责
5. [x] 整体目录结构可直接用于后续开发，无需再调整结构

## 任务结论
符合SDD规范的完整代码目录结构已经建立完成，采用"测试跟随源码"的风格，每个模块都有独立的`__test__`目录，所有现有有效代码已迁移到对应位置，无冗余文件，可以直接开始后续功能开发。

## 已完成工作
### 1. 目录结构已建立完成（测试跟随源码风格）
```
src/
├── agent/          # 代理调度子系统
├── session/        # 会话管理模块
├── group/          # 群组管理子系统
├── task/           # 任务分配子系统
├── message/        # 消息路由子系统
├── context/        # 上下文同步模块
├── protocol/       # ACP协议模块
├── types/          # 全局类型定义
├── config/         # 全局配置
├── core/           # 全局核心逻辑
├── handlers/       # 全局处理器
├── ui/             # UI层
├── utils/          # 工具类
├── __test__/       # 根入口测试
└── extension.ts    # VS Code扩展入口
```
每个模块下都有`core/`、`types/`、`services/`、`__test__`子目录，符合"测试跟随源码"的风格

### 2. 现有代码已迁移到对应模块
- Agent相关代码 → `/src/agent/`
- 会话相关代码 → `/src/session/`
- 协议相关代码 → `/src/protocol/`
- 原有公共模块（config、handlers、ui、utils）保持不变

### 3. 所有模块已添加README.md说明
每个模块目录都有对应的README，明确描述模块职责、对应SDD规范、子目录用途

## 优先级
P0（开发阶段第一个任务，必须先完成才能开始后续功能开发）

## 预计工时
1小时

## 相关人员
开发者：Copilot
评审者：用户

## 变更记录
- 2026-05-06 创建任务，启动代码目录结构梳理与迁移工作
