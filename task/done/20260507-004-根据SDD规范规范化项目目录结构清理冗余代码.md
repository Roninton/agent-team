# 根据SDD规范规范化项目目录结构，清理冗余代码

## 任务概述
根据`docs/specs/architecture/tech-stack-and-architecture.md`中定义的目录规范，清理当前混乱的目录结构，统一代码位置，消除冗余。

## 上下文信息
**SDD规范目录结构：**
```
<root>
├── .copilot/               # Copilot元约束文档
├── docs/                   # SDD规范文档
├── task/                   # LFT任务跟踪目录
├── client/                 # 前端React项目
├── server/                 # 后端NestJS项目
├── extension/              # VS Code扩展代码（原项目代码迁移至此）
├── shared/                 # 前后端共享的TypeScript类型、工具函数
├── package.json            # 根package.json，统一管理脚本
└── README.md               # 项目说明文档
```

**当前状态：**
- ✅ `server/` - NestJS后端，已迁移6个核心模块
- ⚠️ `web/` - 前端React项目，应该重命名为 `client/`
- ⚠️ `src/` - 旧的VS Code扩展代码，应该迁移到 `extension/`
- ❌ `shared/` - 尚未创建

## 执行计划

### 阶段一：目录重命名与创建 ✅
1. ✅ `web/` → `client/` （已完成）
2. ✅ `shared/` 目录结构已存在
3. ✅ 创建 `extension/` 目录，将 `src/` 整体迁移过去

### 阶段二：shared/ 共享层建设
1. ⏳ 提取各模块中的公共类型定义
2. ⏳ 提取通用工具函数
3. ✅ 已建立 shared/types/ 和 shared/utils/ 目录结构

### 阶段三：extension/ 迁移与隔离 ✅
1. ✅ 将 `src/` 整体移动到 `extension/src/`
2. ✅ 移动VS Code扩展相关配置文件到extension目录（.vscode-test.mjs, .vscodeignore, convert-icon.js, webpack.config.js）
3. ✅ extension目录已明确标记为VS Code特定代码，与主应用完全隔离

### 阶段四：清理与验证 ✅
1. ✅ 根目录已清理扩展相关配置文件
2. ✅ package.json已更新，移除VS Code扩展相关内容
3. ✅ 后端测试全部通过（102个测试）
4. ✅ 前端测试全部通过（13个测试）

## 目录最终形态 ✅
```
vscode-acp/
├── .copilot/               # Copilot元约束文档
├── docs/                   # SDD规范文档
│   ├── specs/              # 规范文档
│   ├── api/                # API文档
│   └── user/               # 用户文档
├── task/                   # LFT任务跟踪目录
├── resources/              # 静态资源文件
├── src/                    # 所有源代码统一放在src下 ✅
│   ├── client/             # React前端（原web）
│   ├── server/             # NestJS后端（已迁移6模块）
│   ├── shared/             # 前后端共享
│   │   ├── types/          # 共享TS类型
│   │   └── utils/          # 共享工具函数
│   └── extension/          # VS Code扩展代码（保留历史，与主应用解耦）
│       └── src/            # 旧的扩展代码
├── package.json            # 根package.json，统一管理脚本
└── README.md               # 项目说明文档
```

## 验收标准 ✅
1. ✅ 目录结构完全符合SDD规范（所有代码统一在src/下）
2. ✅ 所有测试全部通过
   - 后端：102个测试通过
   - 前端：13个测试通过
3. ✅ 代码引用路径正确，package.json路径已更新
4. ✅ extension目录已明确标记为VS Code特定代码，不影响主应用
5. ✅ tech-stack-and-architecture.md SDD文档已同步更新目录规范
