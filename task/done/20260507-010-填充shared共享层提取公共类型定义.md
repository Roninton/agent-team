# 任务ID: 20260507-010
## 任务名称：填充shared共享层，提取公共类型定义

## 任务概述
根据SDD规范定义，`src/shared/` 作为前后端共享层，用于存放公共TypeScript类型定义和通用工具函数。目前该目录已建立但内容为空，需要从各模块中提取公共类型定义并统一管理，消除重复定义问题。

## 上下文信息

### 相关规范文档索引
**Level 0 顶层规范：**
- `docs/specs/architecture/tech-stack-and-architecture.md` - 第5节"项目目录结构规范"

**Level 3 协议层规范：**
- `docs/api/rest-api.md` - REST API响应格式、通用错误码
- `docs/api/websocket-api.md` - WebSocket消息格式、事件类型

**Level 2 功能层规范：**
- `docs/specs/features/multi-agent-connection.md` - SessionInfo、AgentInstance等类型定义
- `docs/specs/features/group-chat.md` - 群组、消息类型定义

### 当前状态
- ✅ `src/shared/types/` 目录已创建，**类型定义已实现（6个文件）**
- ✅ `src/shared/utils/` 目录已创建，**工具函数已实现（2个文件）**
- ⚠️ **未编写TDD单元测试**（不符合TDD流程）
- ⚠️ 各模块类型定义分散在各自的 `types/` 目录下，存在重复定义
- ⚠️ 前后端类型不统一，前端类型在 `src/client/src/types/`，后端类型在各模块 `types/`

### TDD执行状态
- ❌ 未先写测试用例（违反TDD先写测试原则）
- ⚠️ 代码已实现但测试缺失，需要补写测试验证正确性

### 技术约束
1. 所有前后端共用的类型必须放在 `src/shared/types/`
2. 纯前端专用的工具函数必须放在 `src/shared/utils/`
3. 各模块只能引用 `src/shared/` 中的公共类型，禁止重复定义
4. 类型命名必须统一，使用 PascalCase
5. 必须导出必须使用 `.ts` 扩展名，禁止使用 `.d.ts`

## 执行计划

### 阶段一：提取公共类型定义
1. **基础类型定义** (`base.types.ts`)
   - 通用响应格式（ApiResponse、ApiError）
   - 分页参数（PaginationParams、PagedResult）
   - 通用错误码枚举（ErrorCode）
   - 基础实体基类（BaseEntity）

2. **代理相关类型** (`agent.types.ts`)
   - AgentInstance、AgentConfig、AgentStatus
   - SessionInfo、SessionState

3. **群组相关类型** (`group.types.ts`)
   - Group、GroupConfig、GroupStatus
   - GroupMember、MemberRole

4. **消息相关类型** (`message.types.ts`)
   - Message、MessageType、MessageStatus
   - ChatMessage、SystemMessage

5. **任务相关类型** (`task.types.ts`)
   - Task、TaskStatus、TaskPriority
   - TaskAssignment

6. **WebSocket消息类型** (`websocket.types.ts`)
   - WebSocketMessage、WsEvent、WsEventType
   - 各类事件数据类型定义

### 阶段二：提取通用工具函数
1. **通用工具** (`common.utils.ts`)
   - ID生成器（generateId）
   - 时间戳工具（formatTime、formatDate）
   - 对象工具（deepClone、deepMerge）
   - 类型守卫工具（isString、isNumber、isObject）

2. **验证工具** (`validation.utils.ts`)
   - 参数验证函数
   - 数据格式验证

### 阶段三：TDD - 编写单元测试
1. **类型测试** (`src/shared/__test__/types.spec.ts`)
   - 验证类型导出完整性
   - 验证类型兼容性
   - 验证枚举值正确性

2. **工具函数测试** (`src/shared/__test__/utils.spec.ts`)
   - `generateId()` - 测试ID唯一性、格式正确性
   - `deepClone()` - 测试深拷贝正确性
   - `deepMerge()` - 测试深度合并正确性
   - `isString/isNumber/isObject` - 测试类型守卫
   - `formatTime/formatDate` - 测试时间格式化
   - 验证工具函数边界条件

3. **运行测试确认失败**
   - 执行 `npm run test -- --run src/shared/__test__/`
   - 确认测试失败（TDD第一步）

### 阶段四：实现与验证
1. 更新各模块引用，改为引用 `src/shared/` 类型
2. 更新前端类型引用
3. 运行测试确保类型正确
4. 确保构建无类型错误
5. 运行测试确认全部通过

## 验收标准
1. ✅ `src/shared/types/` 包含至少6个类型定义文件
2. ✅ `src/shared/utils/` 包含至少2个工具函数文件
3. ✅ `src/shared/__test__/` 包含完整的单元测试（覆盖率 ≥ 90%）
4. ✅ 各模块已更新引用shared中的公共类型
5. ✅ TypeScript编译无类型错误
6. ✅ 所有测试全部通过

## 相关文件路径
- 目标目录：`src/shared/types/`, `src/shared/utils/`
- 参考类型：`src/server/src/modules/agent/types/`, `src/server/src/modules/group/types/`
- 规范文档：`docs/specs/architecture/tech-stack-and-architecture.md`
