# 任务ID: 20260507-013
## 任务名称：统一修复TypeScript错误与测试问题

## 任务概述
当前后端有73个TypeScript编译错误，主要集中在测试文件类型问题、Agent模块遗留代码、API实现与测试不匹配。需要统一修复所有错误，确保构建通过、测试全部跑通。

## 当前错误分类

### 🔴 优先级1 - 核心代码错误（2个）
1. **Agent模块vscode依赖** (`src/modules/agent/types/AgentConfig.ts`)
   - 遗留VS Code扩展代码，项目已转型独立应用
   - 需要移除vscode引用，改用通用类型

2. **CreateAgentDto缺少id字段** (`src/modules/agent/agent.service.ts`)
   - service使用了`createAgentDto.id`但DTO中未定义

### 🟡 优先级2 - 测试文件类型问题（~60个）
1. **Jest mock类型问题**
   - `mockReturnValue` / `mockResolvedValue` 类型不存在
   - 需要使用 `jest.Mocked<>` 或类型断言

2. **Agent Controller测试**（~20个错误）
   - mock类型不正确

### 🟠 优先级3 - 测试与实现不匹配（~10个）
1. **Context Controller测试**
   - 测试调用了 `updateContext`, `clearContext`, `mergeContext` 但Controller未实现
   - 需要同步实现这些API 或 修改测试

2. **Message Controller测试**
   - 测试调用了 `getAllMessages`, `getMessagesBySession`, `updateMessageStatus` 但Controller未实现

## 修复计划

### 阶段一：Agent模块核心修复
1. 移除 `AgentConfig.ts` 中的 vscode 依赖，改用shared层类型
2. 修复 `CreateAgentDto` 添加可选的id字段
3. 验证agent模块无编译错误

### 阶段二：修复测试类型问题
1. 统一使用 `jest.mocked()` 或类型断言修复mock类型
2. 按照实际实现更新测试用例
3. 移除/修改不存在的API调用测试

### 阶段三：补充缺失的API实现（可选）
**方案A - 修改测试匹配现有实现（推荐，快速）**
- 修改测试用例只测试已存在的API
- 缺失API后续按需求添加

**方案B - 补充实现API（完整）**
- Context: 实现 updateContext, clearContext, mergeContext
- Message: 实现 getAllMessages, getMessagesBySession
- 需要同步更新DTO和service层

### 阶段四：验证与集成
1. 运行 `tsc --noEmit` 确认0错误
2. 运行所有Jest测试确认通过
3. 运行前端Vitest测试确认通过
4. 尝试完整构建前后端

## 验收标准
1. ✅ TypeScript编译0错误（前后端）
2. ✅ 所有单元测试通过（Jest + Vitest）
3. ✅ 后端NestJS构建成功
4. ✅ 前端Vite构建成功
5. ✅ shared层类型在前后端正确引用

## 相关文件
- `src/server/src/modules/agent/types/AgentConfig.ts`
- `src/server/src/modules/agent/dto/create-agent.dto.ts`
- `src/server/src/modules/*/__test__/*.spec.ts`
- `src/shared/types/agent.types.ts`
